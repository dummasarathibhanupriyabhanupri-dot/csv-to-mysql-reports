# csv-to-mysql-reports
This project is a Python-based data processing application that reads data from a CSV file, stores the data into a MySQL database, and generates a PDF report from the stored data. 
